### runtime

-   [] add extra property to `routeRequest` like `parsedBody` with prepared body after `handleBody` function. Now there is Object.

defineProperty that mutates incoming request object

-   [] add lazy body handling
